projection;
strict ( 2 );

define behavior for zsc_c_booking alias booking
{
  use create;
  use update;
  use delete;
  use action Change_Status;
  use association _passenger { create; }
}

define behavior for zsc_c_passenger alias passenger
{
  use create;
  use update;
  use delete;

  use association _booking;
}